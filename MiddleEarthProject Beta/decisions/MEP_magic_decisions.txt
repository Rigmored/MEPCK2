targetted_decisions = {

	magic_charm_decision = {
		is_high_prio = yes
		filter = court
		ai_target_filter = court
		from_potential = {
			piety = 10
			OR = {
				trait = caster1
				trait = caster2
				trait = caster3
				trait = caster4
			}
			NOT = {
				OR = {
					has_character_flag = spell_cast1
					has_character_flag = spell_cast2
					has_character_flag = spell_cast3
					has_character_flag = spell_cast4
				}
			}
		}

		potential = {
			age = 16
			NOT = {
				has_character_flag = enchantment_target
			}
		}
		allow = {
			always = yes
		}
		effect = {
			opinion = {
				who = FROM
				modifier = opinion_magic_charmed
			}
			hidden_tooltip = {
				character_event = { id = magic.1001 }
			}
			FROM = {
				piety = -10
				hidden_tooltip = {
					character_event = { id = magic.10 }
					character_event = { id = magic.102 }
				}
			}
		}
		revoke_allowed = {
			always = no
		}
		ai_will_do = {
			factor = 0
		}
	}
}


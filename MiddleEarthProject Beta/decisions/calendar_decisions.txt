decisions = {
	turn_off_calendar = {
		potential = {
			ai = no
			#ai cannot use dynasty decisions
			NOT = { has_global_flag = calendar_events_disabled }
			has_character_flag = special_decisions_open
		}
		effect = { set_global_flag = calendar_events_disabled }
	}
	turn_on_calendar = {
		potential = {
			ai = no
			#ai cannot use dynasty decisions
			has_global_flag = calendar_events_disabled
			has_character_flag = special_decisions_open
		}
		effect = { clr_global_flag = calendar_events_disabled }
	}
}
